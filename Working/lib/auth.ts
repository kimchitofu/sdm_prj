// NextAuth configuration removed.
// The application now uses Firebase Authentication (client-side).
// Keep a placeholder export so imports don't fail during build.

export const authOptions = {} as any